# Mandatory Isolation Gate

SMART OS must never auto-edit any sibling repository. Research access and write access are separate permissions. A target repository may be written only after an explicit export/attach command names that target. Every generated project must use a unique workspace, secrets namespace, logs, provenance ledger, and decision log. Secrets must never be committed.
